# Sub2 Smart Group

面向个人 sub2api 后台的 Tampermonkey 管理工具。它在不修改 sub2api 源码、前端文件或容器镜像的前提下，提供账号健康状态、真实请求中的客户端 Key 名称、真实路由记录、首字耗时、配置审计、优先级、日配额、手动余额查询、账号创建和分组感知模型管理能力。

从 `v2.0.0` 开始，项目已经移除全部 AIHub 页面匹配和业务代码，成为纯 sub2 专属工具。原有 `sub2-smart-group:` 存储键、GitHub Raw 更新地址和 userscript namespace 保持不变，升级后已有面板偏好不会因此丢失。

## 核心原则：不主动测活

上游禁止测活时，本脚本不会定时调用 test / probe，也不会为了判断健康状态发送模型请求。自动刷新只读取当前 sub2 后台的本机 Admin API：

- 账号健康和冷却状态；
- 分组与平台配置；
- 今日真实请求数和花费；
- 最近成功 / 失败请求、真实故障转移记录和已记录的流式请求首字耗时。

账号卡片中的“模型”按钮默认只读取 sub2 已保存的模型。只有用户明确点击“拉取并同步上游”时，才会通过 sub2 官方接口访问对应上游；返回模型会按账号真实分组平台过滤，再由用户确认后持久化。

添加账号也只从明确点击开始。脚本把当次输入的地址和 API Key 发送到当前 sub2 后台，由官方预览接口识别 OpenAI / GPT 或 Anthropic / Claude；确认后才调用官方创建接口，不从浏览器直接访问候选上游。

余额查询同样不会自动执行。脚本先由当前 API Key 账号的上游地址和协议注册表确定方法：sub2api 只在用户点击后单独导出当前账号的模型 Key；New API 只使用与当前 origin 绑定的 Access Token + User ID 查账号余额。

## 安装与自动更新

1. 安装 Tampermonkey。
2. 打开以下 GitHub Raw 地址并安装：
   `https://raw.githubusercontent.com/hong594/sub2-smart-group/main/sub2-smart-group.user.js`
3. 登录 sub2api 后台，右下角会出现蓝色 `S2` 按钮。

脚本默认匹配：

```text
http://localhost:18080/*
http://127.0.0.1:18080/*
http://localhost:8080/*
http://127.0.0.1:8080/*
```

如果使用内网 IP、自定义域名或 HTTPS，请在 Tampermonkey 的本脚本设置中添加“用户匹配”，例如：

```text
http://192.168.x.x:18080/*
https://your-sub2-domain.com/*
```

脚本保留了 GitHub Raw `@updateURL` 和 `@downloadURL`。自动检查频率由 Tampermonkey 自身控制，也可手动执行“检查用户脚本更新”。

## 当前版本

`v2.10.0`

`v2.10.0` 新增：

- 移除单独输入完整客户端 Key 的查询流程，改为在“真实请求历史”每条记录中直接显示 Key 名称；名称按请求中的 `api_key_id + user_id` 从 Admin API 读取并在内存中缓存，无法读取时显示 Key ID。
- 只读取 Key 元数据中的名称，不显示或保存完整客户端 Key，也不会访问上游。

`v2.8.0` 新增：

- 增加“同步 GPT 模型”批量操作：只按真实 `group.platform=openai` 选择分组证据完整的账号，逐个读取上游模型并只保留 GPT / OpenAI 文本模型；空结果、冲突分组和失败账号不会写入。
- 批量同步开始前和发生旧系统映射删除前都会明确确认，写入仍只提交单账号 `bulk-update` 的完整 `model_mapping`，并在成功后回读校验。

`v2.7.1` 新增：

- 余额设置先根据当前账号确定 sub2api 模型 Key 余额或 New API 账号余额，不再让用户选择模式、来源或 origin；
- sub2api 直接使用 sub2 已保存的当前账号模型 Key；New API 固定先读取 `/api/status` 的动态 `quota_per_unit`，再用 Access Token + User ID 请求 `/api/user/self`，不使用或回退到模型 Key 额度；
- 余额设置旁增加“导入余额”，可在浏览器本地选择 All API Hub JSON，按当前账号 hostname 唯一匹配并批量填写 New API 的 Access Token + User ID；导入前显示数量并确认，导入本身不访问上游；
- 多条同 hostname 只有精确名称能唯一消歧时才写入；歧义、无匹配、禁用、格式错误和协议冲突均跳过并只报告数量；
- 预览分开显示将补充、将纠正、信息已齐全、可直接查询、歧义、无匹配和其它跳过；已齐全凭据不被覆盖；
- All API Hub 的 sub2api `account_info.access_token` 不当作 `/v1/usage` Key 导入，已识别 sub2api 账号继续使用 sub2 已保存的模型 Key 查询；
- 导入后的凭据仍按当前 sub2 地址 + 账号 ID 写入 Tampermonkey，列表控制器只保留无秘密摘要；只改低余额阈值时会局部读取并保留隐藏凭据。

`v2.7.0`

`v2.7.0` 新增：

- 建立了 `apikey` 账号从 sub2 单账号导出临时 Key 的查询边界；`v2.7.1` 起该边界仅用于 sub2api；
- 单账号导出会复核名称、平台、类型和完整规范化上游地址；导出响应本身没有账号 ID，因此不作虚假的响应 ID 核对声明；
- New API 的动态 quota 换算和单账号 Key 导出边界在该版本建立；`v2.7.1` 起 New API 固定查账号余额，单账号模型 Key 导出仅属于 sub2api；
- 内部旧 `mode` 标签和旧凭据格式保留为读取与回滚兼容数据，不再是界面中的可选概念；
- 当前 29 个上游域名使用精确 `@connect` 注册表，未知协议域名不自动猜测；查询失败保留上一次成功余额证据。

`v2.6.0` 新增：

- 余额、容量和日配额共用一个互斥编辑器状态，切换账号或编辑类型时不会叠加面板，筛选和排序仍可正常重建列表；
- 配置审计统一使用规范化分组和审计结果，不再出现对象文本、重复分组或混入审计计数的容量建议；
- 最近命中变化按“分组 ID + 平台 + 请求模型”分别比较，交错的其它路由不会制造或掩盖切换事件；
- 请求历史展示真实 `first_token_ms`，账号卡片汇总滚动 24 小时 P90、P50、最新值和样本数，TTFT 只作为人工判断依据；
- 支持从上游地址和当次 API Key 识别并创建 OpenAI / GPT 或 Anthropic / Claude `apikey` 账号；
- 创建前必须选择平台匹配的启用分组，使用并发 `1`、过滤后的同族模型和账号级保守优先级，创建后回读实际组内优先级；
- 创建密钥只存在于密码输入框和同源预览 / 创建请求，改变地址、取消、终止失败或成功都会清除，并用临时幂等键防止重复提交；
- 手动模型同步按真实 `group.platform` 隔离 GPT 与 Claude，只替换系统拥有的同名映射，保留手工映射和其它凭据字段。

`v2.5.0` 新增：

- 最初允许每个账号单独配置 `sub2api` 或 `newapi` 余额来源；当前版本仅保留旧 GM 数据的读取和回滚兼容，界面方法改为由账号派生；
- `sub2api` 固定请求 `/v1/usage`，早期 New API `/api/user/self` 的固定换算方式已在 `v2.7.0` 改为读取站点动态 quota 配置；
- 余额凭据按“当前 sub2 地址 + 账号 ID”分别存入 Tampermonkey，不回退到网页 `localStorage`，已保存密钥不会回填到页面 DOM；
- 当时编辑器中新输入的凭据与来源类型和规范化上游域名绑定；当前版本已改为账号派生方法和 origin；
- 只允许预先声明的 HTTPS 域名和标准端口，拒绝路径、查询参数、自定义端口与重定向；
- 只由真实用户点击触发余额请求，不轮询、不自动重试测活，并且不携带第三方 Cookie；
- 余额、今日消费与按今日平均速率估算的续航并列展示，低于账号阈值时标红；
- 今日至少 3 次请求且单均费用不低于 `$0.50` 时提示“单均偏高”，但不把它解释成上游倍率；
- 今日统计读取失败、超过 30 秒未更新、跨过本地日期、窗口不足 1 小时或余额不是 USD 时不计算续航；编辑器打开时证据也会原位过期，不重建账号列表或丢失草稿。

`v2.3.0` 新增：

- 事件中心汇总真实 429、403、5xx、账号冷却和最近命中变化；
- 打开事件中心时按需读取近 24 小时成功率、最终失败和故障转移次数；
- 请求或错误记录超过单次读取上限时明确显示“样本”或“下限”，不冒充完整统计；
- 可靠性事件只保存在当前浏览器，可选择保留 24 小时、7 天或 30 天；
- 过期事件在刷新和切换策略时自动清理，同时限制最多 500 条并支持手动清空。

`v2.2.0` 新增：

- 最近 30 分钟内最多 30 条成功/失败真实请求历史；
- 按模型、分组、最终账号和路由结果筛选；
- 点击历史请求后按请求 ID 读取关联错误并回放路由链；
- 账号级人工并发容量编辑器，输入与实际应用分离；
- 降容低于当前占用时显示排队风险，不中断已有请求；
- 容量更新只提交官方 `concurrency` 字段，不改分组、凭据、优先级或调度权重。

`v2.1.0` 新增：

- 账号当前容量、并发占用、负载和排队快照；
- 面板级“路由诊断”抽屉；
- 最近请求的多账号故障转移链；
- 更高优先级账号的候选资格分析；
- 按需核对 sub2 已保存模型，不自动访问上游；
- 对详情链、摘要链、历史证据和当前快照作明确区分。

从 `v2.0.0` 起已移除：

- `aihub.top` 页面匹配；
- AIHub 智能分组、自动切组和监控逻辑；
- AIHub 余额、倍率、Key 分组增强逻辑；
- `unsafeWindow` 权限；
- 仓库中的 AIHub 原版备份文件。

## 面板功能

### 账号状态与筛选

- 正常、注意、不可用、已停用四色健康总览；
- 按分组或全部账号展示；
- 按分组、平台和健康状态筛选；
- 按健康度、优先级、今日花费或名称排序；
- 搜索账号名称、平台和分组；
- 五个筛选项采用紧凑单行布局。

### 配置审计与互斥编辑器

- 余额、容量和日配额在整个列表中最多打开一个编辑器；再次点击当前编辑器会关闭，点击其它账号或类型会直接切换；
- 编辑器打开时仍可筛选、排序和切换视图；账号仍可见时保留草稿与焦点，被筛掉时清除未保存的敏感输入；
- 配置审计使用规范化后的分组名称、类别、严重级别和证据，不直接渲染后端原始分组对象；
- 容量建议独立展示，不计入审计问题数量。

### 最近命中与路由原因

- 最近一次成功请求实际使用的账号显示“最近命中”；
- 优先读取 `/admin/ops/requests`，运维接口不可用时才回退到 `last_used_at`；
- 展示真实的 401、403、429、502、503 等上游故障转移记录；
- 优先通过客户端请求 ID 关联失败账号和最终成功账号；
- 明确区分“已证实”“当前状态 / 配置判断”和“推测”；
- 对没有完整候选淘汰轨迹的情况，不会武断声称某账号一定没有被请求。

### 容量与路由诊断

- 读取 `/admin/ops/concurrency`，展示账号当前并发占用、最大容量、负载和排队数；
- 面板汇总按账号去重后的当前并发与总容量；
- 容量数据明确标记为当前快照，不冒充历史请求发生时的并发；
- “路由诊断”抽屉展示最近成功请求、实际失败账号和最终成功账号；
- 优先读取关联错误详情中的 `upstream_errors` 形成详情链；
- 详情接口不可用时降级成摘要链，并明确提示可能缺少中间尝试；
- 对更高优先级账号检查平台、分组、到期状态、当前健康、总/日/周配额和容量；
- 模型资格优先按 sub2 官方 `model_mapping` 语义判断，包括大小写、末尾 `*` 通配符和 Gemini 模型别名；
- 旧版账号响应未暴露模型映射时，可点击“核对保存模型”读取本机 sub2 数据，不会自动同步上游；
- 会话粘连、调度权重和未持久化的历史候选评分仍标为未知，不做过度推断。

### 请求历史与路由回放

- 展示最近 30 分钟内最多 30 条真实成功和失败请求；
- 展示请求时间、Key 名称、模型、分组、最终账号、状态、总耗时和可用的真实首字耗时；
- 可按模型、分组、最终账号和直接成功 / 故障转移 / 最终失败筛选；
- 只有点击某条历史记录时，才按请求 ID 读取其关联上游错误和最多 8 条详情；
- 详情缺失时继续保持“部分详情 / 摘要 / 信息不足”证据边界；
- 请求回放只读取本机 Ops 日志，不会重发请求或访问模型上游；
- 账号 TTFT 汇总读取最新最多 1000 条流式记录，展示滚动 24 小时 P90、P50、最新样本、样本数和分页覆盖状态；
- 缺失 `first_token_ms` 时不会用总耗时补值，也不会根据 TTFT 自动摘出、挂回或调整账号。

### 事件中心与可靠性统计

- 记录请求级或上游尝试级的 429、403 和 5xx 真实错误；
- 对比连续账号快照，记录进入冷却、冷却更新、冷却结束和同一完整路由范围内的最近命中账号变化；
- 命中变化要求分组 ID、平台和请求模型完全一致；其它分组或模型的交错请求不会参与比较；
- 打开事件中心后按需读取近 24 小时请求，不把 24 小时统计加入每 10 秒主刷新；
- 展示成功率、最终失败数、已识别故障转移成功次数和受关注状态码数量；
- 请求或错误分页未覆盖完整时，将故障转移数标为下限并说明样本边界；
- 本地事件默认保留 7 天，可切换为 24 小时或 30 天；
- 每次合并事件都会去重、删除过期记录并只保留最新 500 条，也可手动清空；
- 本地事件不会上传到 GitHub、sub2api 或任何第三方服务。

### 账号路由管理

- 紫色 `P数字` 标签突出账号优先级；
- 手动提升或降低账号优先级；
- 摘出或挂回调度池；
- 清除限流、过载或临时熔断冷却；
- 只读展示池模式，不提供容易误用的快捷切换；
- 显示组内优先级和账号所属分组。

### 一键添加 API Key 账号

- 顶部 `+` 按钮打开添加账号窗口，第一版只支持 OpenAI / GPT 与 Anthropic / Claude 的 `apikey` 账号；
- 地址只接受 HTTPS，或本机 `localhost`、`127.0.0.1`、`[::1]` 的 HTTP；拒绝 URL 凭据、查询参数、片段和其它协议；
- 一次识别会通过当前 sub2 的同源预览接口分别尝试两种协议，必须恰好有一种返回允许的同族模型；
- 目标分组只依据真实 `group.platform`：当前兼容分组优先、唯一兼容分组自动选择、多个分组要求手选、没有兼容分组时禁止创建；
- 确认页显示生成名称、平台、模型数、并发 `1` 和账号级优先级；创建后以后端回读的组内优先级为准；
- 创建请求带一次性幂等键；提交期间禁止重复点击、关闭或最小化，结果不确定时可用同一幂等键重试。

### 人工容量调节

- 每个账号提供独立“容量”编辑器，显示配置容量、当前占用和排队；
- `-` / `+` 只调整输入值，必须点击“应用”才写入；
- 只允许 `1` 到 `10000` 的正整数，避免误设成无限制容量；
- 新容量低于当前占用时明确提示：已有请求不会中断，新请求可能暂时排队；
- 更新只向官方账号接口提交 `{ concurrency }`，不会连带修改分组、凭据、优先级、`load_factor` 或池模式；
- 保存失败时保留编辑器和原输入，成功后重新读取账号及 Ops 容量快照；
- 不提供自动扩缩容或批量容量修改。

### 日配额

API Key / Bedrock 账号支持：

- 查看当日已用金额和每日美元限额；
- 使用率达到 80% 或 100% 时显示警告色；
- 手动设置或取消每日限额；
- 保存前读取账号最新详情，完整保留其它 `extra` 字段；
- 取消限制时只清理日配额相关字段。

### 余额与消费风险

- 余额方法由当前 `apikey` 账号的规范化上游 origin 和精确 hostname 协议注册表唯一确定；未注册、协议为空或地址无效时只显示固定原因，不猜测、不探测；
- sub2api 不需要填凭据，只在点击“查余额”后单账号导出并临时使用 sub2 已保存的模型 Key 请求 `/v1/usage`；
- New API 固定查账号余额：先读取公开 `/api/status` 的动态 `quota_per_unit`，再使用 Access Token + 正整数 User ID 请求 `/api/user/self`；缺少凭据或查询失败都不切换方法；
- 旧 `mode` 标签仅作 GM 数据兼容；旧 sub2api 手工 Key 不再参与请求，但不会因升级、导入或修改阈值被删除；
- 设置界面不回填已存凭据，只显示当前方法和尚缺字段；只修改阈值时局部合并已存完整配置；
- 账号卡片始终展示 sub2 的今日请求数、今日花费和单均费用；
- 余额查询成功后，USD 余额可结合今日截至当前的平均花费速率粗略估算剩余小时；
- 续航是基于今日历史速率的估算，不代表实时上游倍率，也不是消费承诺；
- 今日统计不可用或窗口不足时明确显示信息不足，不把缓存值冒充当前事实；
- 查询结果只保留在当前页面内存中，刷新页面后需再次手动查询；
- 余额设置可逐账号明确清除，清除操作需要用户确认；sub2api 清除后仍可直接查询，New API 清除后变为需要补充。
- “导入余额”只读取用户选择的本地 JSON，不上传、不调用上游；匹配按 hostname，多个候选必须由精确账号名唯一消歧，已有低余额阈值会保留。

### 模型与上游链接

- 查看 sub2 已保存的账号模型并搜索；
- 只有明确点击后才拉取上游模型，缺失、冲突或不支持的真实分组平台会在请求前阻止同步；
- “同步 GPT 模型”会逐个处理所有真实归属于 `group.platform=openai` 的 GPT 分组账号，不依赖分组名称猜测；
- OpenAI 分组只保留 GPT / ChatGPT / o 系列 / Codex 文本模型，Anthropic 分组只保留 Claude 模型；
- 同步会新增当前允许的同名映射、删除过期或错族的系统同名映射，并保留手工别名；有删除时必须再次确认；
- 持久化只通过单账号 `bulk-update` 合并完整 `model_mapping`，不重建或覆盖其它凭据字段；
- 账号名称可打开 `base_url` 对应的站点根地址；
- 只有名称文字和外链图标可以点击，标题栏空白处不会误触；
- 链接只允许 HTTP / HTTPS，并使用 `noopener noreferrer`。

### 布局与刷新

- 约 430px 宽的竖向单列面板；
- 桌面端高度约为视口的 88%，最高 1000px，并受视口剩余空间限制；
- 账号列表内部滚动并保持滚动位置；
- 面板可见且标签页位于前台时，每 10 秒刷新本机详情；
- 最小化、后台标签、添加账号窗口或账号容量 / 日配额 / 余额编辑与余额查询期间暂停刷新；
- 近 24 小时可靠性只在事件中心打开时按需读取，且一分钟内复用缓存；
- 运维日志读取有超时保护，不会阻塞账号列表主刷新。

## API 边界

主要只读接口：

```text
GET  /api/v1/admin/accounts
GET  /api/v1/admin/accounts/:id
GET  /api/v1/admin/groups/all
POST /api/v1/admin/accounts/today-stats/batch
GET  /api/v1/admin/ops/requests
GET  /api/v1/admin/ops/upstream-errors
GET  /api/v1/admin/ops/upstream-errors/:id
GET  /api/v1/admin/ops/concurrency
GET  /api/v1/admin/usage
GET  /api/v1/admin/accounts/:id/models
GET  /api/v1/admin/users/:id/api-keys                 读取请求历史中 API Key 的名称
```

只有用户明确点击时才调用上游模型读取接口；这两个 POST 本身不持久化账号配置：

```text
POST /api/v1/admin/accounts/models/sync-upstream-preview
POST /api/v1/admin/accounts/:id/models/sync-upstream
```

只有用户确认操作时才调用写接口：

```text
POST /api/v1/admin/accounts
POST /api/v1/admin/accounts/bulk-update
POST /api/v1/admin/accounts/:id/schedulable
POST /api/v1/admin/accounts/:id/recover-state
PUT  /api/v1/admin/accounts/:id
```

OpenAI API Key 账号创建后，sub2 官方创建端点可能异步执行 Responses 能力探测；这是后端既有副作用，不是脚本调用 test / probe 接口。

只有用户真实点击余额按钮时才访问外部接口：

```text
GET /api/v1/admin/accounts/data?ids=<current-id>&include_proxies=false
GET <allowlisted-https-origin>/v1/usage
GET <allowlisted-https-origin>/api/status
GET <allowlisted-https-origin>/api/user/self
```

## 安全边界

- 复用当前 sub2 后台的 `localStorage.auth_token`，不会在仓库中保存管理员凭据；
- sub2api 查询只在可信点击后调用带单个账号 ID 的只读导出接口，并在发送 Key 前复核单条响应、名称、平台、类型和完整规范化地址；
- 导出的 sub2api Key、地址或协议副本不写入 Tampermonkey、DOM、`localStorage`、日志、诊断、剪贴板、文件或 GitHub；完成后清空局部 Key 和导出对象引用；
- 请求历史 Key 名称只根据真实请求中的 `api_key_id` 和 `user_id` 读取 API Key 元数据；完整客户端 Key 不写入 Tampermonkey、DOM、`localStorage`、日志、诊断、剪贴板、文件或 GitHub；
- New API 账号余额凭据以未加密形式按“当前 sub2 地址 + 账号 ID”存入 Tampermonkey；已保存秘密不回填 DOM，新输入凭据只绑定当前方法解析出的规范化 origin；
- 本地导入的原始 JSON、Access Token、User ID 和原始响应不会进入 DOM 文本、控制器状态、日志、诊断、剪贴板、文件或仓库；确认取消或文件 schema 无效时零写入；
- 新账号 API Key 不写入 Tampermonkey、`localStorage`、日志、诊断、剪贴板、页面结果或仓库文件，只进入当次同源预览 / 创建请求，并在完成、取消或终止失败后清除；
- 账号创建不从浏览器直连候选上游，必须由当前 sub2 后台预览；没有唯一平台和一个兼容启用分组时不创建；
- 模型同步只提交完整 `model_mapping` 合并值，不发送从详情响应中取得的脱敏凭据快照；
- 外部请求只允许脚本元数据中声明的 29 个精确 HTTPS 域名、标准端口和固定路径，并拒绝重定向；未知或协议为空的域名不构造查询；
- 外部请求不携带第三方 Cookie，且必须来自余额按钮的真实用户点击；
- 不主动测活、不自动同步模型；
- TTFT 仅作只读证据，不自动调整调度、优先级、容量、池模式、配额或账号状态；
- 不提供伪通用余额查询：只实现本文明确列出的 `sub2api` 与 `newapi` 两种协议；
- 不修改 sub2api 源码、镜像、容器或数据库结构。

## 验证

- `node --check sub2-smart-group.user.js`
- `git diff --check`
- Node 纯函数断言覆盖互斥编辑器、规范化审计、路由范围、TTFT 百分位、模型族过滤与映射合并、账号 URL / 平台 / 分组解析、幂等创建载荷，以及余额方法解析、域名注册表、单账号导出绑定、New API 账号 quota 换算、阈值合并、导入分类和既有可靠性 / 容量 / 配额边界。
- 本地浏览器 harness 覆盖两次平台预览、一次创建、幂等头、分组 / 优先级 / 并发 / 模型载荷、密钥清除、创建后回读，以及移动端和桌面端分组滚动与不重叠布局。
